var isAnagram = function(t, o) {
    return (f=>f(t)==f(o))(x=>x.toLowerCase().split('').sort().join``)
    };
    
    // write the function isAnagram
    var isAnagram = function(t, o) {
      return t.toLowerCase().split('').sort().join``==o.toLowerCase().split('').sort().join``
    };